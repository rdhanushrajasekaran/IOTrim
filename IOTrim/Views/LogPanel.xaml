<UserControl x:Class="IOTrim.Views.LogPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{DynamicResource AppSurfaceBrush}"
            BorderBrush="{DynamicResource AppBorderBrush}"
            BorderThickness="1"
            CornerRadius="12">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{DynamicResource AppAccentBrush}"
                    CornerRadius="12,12,0,0">

                <DockPanel Margin="12,0">

                    <TextBlock Text="Machine Logs"
                               Foreground="White"
                               FontWeight="SemiBold"
                               FontSize="14"
                               VerticalAlignment="Center"
                               DockPanel.Dock="Left"/>

                    <Button Content="Clear"
                    Style="{StaticResource SearchBtnStyle}"
                    Width="80"
                    Background="{DynamicResource AppWarningBrush}"
                    HorizontalAlignment="Right"
                    Height="28"
                    Click="BtnClearLog_Click"/>
                </DockPanel>

            </Border>

            <!-- Log Content -->
            <TextBox x:Name="txtLogs"
                     Grid.Row="1"
                     Margin="20"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="Transparent"
                     BorderThickness="0"
                     FontFamily="Consolas"
                     FontSize="14.5"/>

            <!-- Footer -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="10,0">

                <Button Content="Close"
                        Style="{StaticResource SearchBtnStyle}"
                        Width="80"
                        Background="{DynamicResource AppExportBrush}"
                        Height="28"
                        Click="BtnCloseLog_Click"/>

            </StackPanel>

        </Grid>

    </Border>

</UserControl>